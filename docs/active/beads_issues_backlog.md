# Beads Issues: Document Chunking Backlog

This backlog contains 26 beads issues ready to be assigned to agents configured with the `beads-mem` skill.

---

## [Beads-Mem] Split and tag granular sections in docs/readme.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/readme.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/records/dev-guide.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/records/dev-guide.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/records/change-log.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/records/change-log.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/records/bug-fixing-guide.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/records/bug-fixing-guide.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/records/adrs.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/records/adrs.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/records/decisions.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/records/decisions.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/context/architecture.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/context/architecture.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/context/tech-stack.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/context/tech-stack.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/context/initial-requirements.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/context/initial-requirements.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/context/domain-terms.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/context/domain-terms.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/archive/system-overview.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/archive/system-overview.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/module-overview.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/module-overview.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/visualizer-logic.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/visualizer-logic.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/game-mechanics.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/game-mechanics.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/test-plan.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/test-plan.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/course-modules/module-4.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/course-modules/module-4.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/course-modules/module-1.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/course-modules/module-1.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/course-modules/module-5.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/course-modules/module-5.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/course-modules/module-2.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/course-modules/module-2.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/specs/course-modules/module-3.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/specs/course-modules/module-3.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/active/roadmap.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/active/roadmap.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/active/handover-hardening-sprint-1/sprint-backlog.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/active/handover-hardening-sprint-1/sprint-backlog.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/active/handover-hardening-sprint-1/dod.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/active/handover-hardening-sprint-1/dod.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/reviews/sprint-review.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/reviews/sprint-review.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/reviews/refactoring-plan-2026-03.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/reviews/refactoring-plan-2026-03.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

## [Beads-Mem] Split and tag granular sections in docs/reviews/qa-report.md

**Issue Content / Prompt:**

```markdown
Examine one selected md file: `docs/reviews/qa-report.md`.
From that, split into the smallest sections (like #, ##, ### or even smaller). Each section and sub-section can be splitted into beads (smallest task which can be done with one-shot LLM to edit).

:::

**Agent Instructions (beads-mem):**
In this individual beads issue, it must require agent coding to use Agent Skill: `beads-mem` to examine and split that doc by adding beads (HTML Comment UUIDs like `<!-- beads-id: ... -->`) into individual sections / subsections.
```

---

